Add your background video here.

Filename: hero-loop.mp4

Requirements:
- Format: MP4 (H.264) works in all modern browsers
- Keep file size reasonable (e.g. 2–10 MB) for fast loading
- Short loop (15–30 seconds) works best
- Landscape orientation; it will be cropped to cover the hero area (object-fit: cover)

The video plays muted and loops behind the hero text. If the file is missing, the gradient background will still show.
